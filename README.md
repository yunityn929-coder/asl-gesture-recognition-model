# asl-gesture-recognition-model

Before retraining or re-evaluating the MLP model, read [NOTES.md](NOTES.md) —
random row-level train/val splits silently reintroduce a data-leakage bug
that once reported a false 100% validation accuracy.